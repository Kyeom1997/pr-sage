# Recall sheet — human review comments vs pr-sage findings

Each PR was reviewed at its FIRST commit (the state human reviewers saw).
`hit` means pr-sage flagged the same file within ±10 lines of a human comment —
an approximation: verify each pair by hand before quoting a recall number.

## fastify/fastify#6785 — docs(ecosystem): add @thecodepace/fastify-http-query (0/1 matched)
https://github.com/fastify/fastify/pull/6785

- [ ] `docs/Guides/Ecosystem.md:237` (@Puppo) ```suggestion   method with a body). ```

## fastify/fastify#6832 — feat: rfc10008 http query method (0/7 matched)
https://github.com/fastify/fastify/pull/6832

- [ ] `lib/errors.js:453` (@climba03003) It allows future method that requires the same requirement of `query`.
- [ ] `lib/handle-request.js:45` (@jsumners) I have a suspicion that the parsed content-type header object should be used here.
- [ ] `lib/handle-request.js:62` (@jsumners) This is only validating one part of the "is missing or is inconsistent with the request content." requirement. So, technically, in order for us to provide suppo
- [ ] `lib/handle-request.js:112` (@jsumners) There should be a docblock on this at least. It doesn't really seem reusable, because the content is clearly tied to the new method, so it really doesn't need t
- [ ] `lib/handle-request.js:112` (@climba03003) This function is used in two place. 1. Used to check whether using the Content-Type parser. 2. Used in Query method.  I extract it as a function because I do no
- [ ] `lib/handle-request.js:62` (@climba03003) It is partially addressed the `missing` part, for the `inconsistent` part is handled by content type parser. You can check the test `should return 400 if invali
- [ ] `lib/handle-request.js:45` (@climba03003) Invalid or Unsupported Content-Type header is checked in later stage. It is the function we already have.
